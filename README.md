# luci-app-adguardhome

`luci-app-adguardhome` 2.4.0-r10，兼容基线为 ImmortalWrt 25.12.1（APK）。

本项目是去 Lua、去 CBI 的纯 LuCI JavaScript + ucode RPC 实现。AdGuard Home 核心、二进制、官方小写服务和官方 UCI 主配置均由 ImmortalWrt 的 `adguardhome` 软件包提供，本插件只负责 LuCI 管理、DNS 集成、配置协调和可选的内存数据运行。

## 唯一 UCI 配置

活动配置只使用一个 UCI 文件：`/etc/config/adguardhome`。运行时不创建或使用 `/etc/config/AdGuardHome`，也不存在第二个 `enabled` 或第二个 `work_dir`。

标准配置格式如下：

```uci
config adguardhome 'config'
	option enabled '0'
	option config_file '/etc/AdGuardHome/AdGuardHome.yaml'
	option work_dir '/etc/AdGuardHome'
	option verbose '0'

config luci 'luci'
	option redirect 'dnsmasq-upstream'
	option run_from_memory '0'
	option memory_writeback_interval '60'
```

`config` 是官方核心配置段：

- `enabled` 是唯一的服务启用开关。
- `work_dir` 是唯一的官方工作目录。
- `config_file` 始终为 `<work_dir>/AdGuardHome.yaml`；修改工作目录时由插件同步更新，不需要再单独指定 YAML 路径。
- `verbose` 直接控制官方服务的详细日志选项。

`luci` 段只保存插件自己的 DNS 模式、内存模式和回写周期。插件会保留官方配置中不属于它管理范围的选项。

默认工作目录为 `/etc/AdGuardHome`。自定义工作目录仅允许使用 `/etc/AdGuardHome-*`，或 `/mnt` 挂载盘下以 `AdGuardHome`/`AdGuardHome-*` 命名的专用叶目录，以免官方服务改动系统目录的所有权。

## 核心与内存模式

- 核心由官方 `/etc/init.d/adguardhome` 服务管理，并以官方 `adguardhome` 用户和组运行（ImmortalWrt 25.12.1 默认 UID/GID 853）。插件不会替换 `/usr/bin/AdGuardHome` 或官方服务文件。
- 开启“从内存运行”后，只把持久工作目录中的 `data` 内容载入 RAM，并将 RAM 中的 `data` 绑定到 `<work_dir>/data`。
- 官方 `work_dir` 及 UCI 中的 `work_dir` 始终保持持久路径；`AdGuardHome.yaml` 始终从 `<work_dir>/AdGuardHome.yaml` 读取和修改；主程序、YAML 和整个官方工作目录均不会复制到内存。
- 默认每 60 分钟回写一次，可设置为 1–10080 分钟；设置为 `0` 可关闭周期回写。
- 手动和周期回写直接用 `cp` 把 RAM 中 `data` 的内容写回持久 `data`，同名对象直接覆盖，不先清空目标目录，也不停止或重启核心、插件服务或 DNS 服务。
- 内存模式不建立持久事务日志或回写快照，也不承诺并发快照一致性或极端断电可靠性；断电时可能丢失上次成功回写后的数据。启动准备若在发布活动状态前中断，下次启动只丢弃经过校验的插件专用临时目录并从持久 `data` 重新载入。
- 内存模式使用系统已有的 tmpfs、挂载能力和 BusyBox 基础工具，不新增 `rsync`、cron、`coreutils-stat` 或 `coreutils-timeout` 等依赖。有界操作由插件自身的 shell 监控完成，不需要任何替代的 timeout 软件包。

## DNS 集成模式

插件支持三种模式，实际 AdGuard Home DNS 端口始终从当前 YAML 的 `dns.port` 动态读取；`53335` 只是默认模板值。

- `none`：不修改 dnsmasq 或防火墙。AdGuard Home 可以直接监听 53 端口。
- `redirect`：使用 fw4 将路由器收到的 53 端口 DNS 请求重定向到 YAML 中的实际 AdGuard Home DNS 端口。
- `dnsmasq-upstream`：保留 dnsmasq 的 53 端口监听，并把 dnsmasq 上游指向 YAML 中的实际 AdGuard Home DNS 端口。此模式要求系统只有一个 dnsmasq UCI 实例；进入模式时保留条件转发，只添加精确的 `127.0.0.1#<dns.port>` 上游并设置 `noresolv=1`。检测到多个 dnsmasq 实例、既有普通上游或 `/#/` 通配上游时会拒绝接管。

`redirect` 和 `dnsmasq-upstream` 要求 YAML 的 `dns.port` 不是 53；`none` 模式可使用 53。停用插件或离开 `dnsmasq-upstream` 时会删除插件记录的精确上游并取消 `noresolv`，不修改 `resolvfile`；切换其他模式时只撤销插件自己创建的 DNS 或防火墙项。

## LuCI 页面

LuCI 菜单入口统一为小写 `/admin/services/adguardhome`，包含三个页签：

- “设置”：显示运行状态，控制启停，修改 DNS 模式、官方工作目录、详细日志选项、内存模式及回写周期，并可修改 YAML 中唯一管理账号的用户名、密码或两者。密码在浏览器端以 BCrypt cost 10 生成哈希，路由器不会收到密码明文。
- “运行日志”：分别显示官方核心日志和插件协调器日志，均按时间倒序排列，最新记录位于最上方。
- “YAML 配置”：编辑、校验、保存并应用 `<work_dir>/AdGuardHome.yaml`。读取完整 YAML 需要插件写权限，避免只读账号取得密码哈希或内嵌私钥；只有校验成功并安全落盘后才应用，失败不会留下半写入的活动配置。

“恢复模板”只把软件包模板载入 YAML 编辑框，既不会立即写入文件，也不会立即应用或重启服务。用户仍停留在编辑流程中，可继续修改；只有随后点击“校验、保存并应用”才会生效。默认模板不启用 HTTPS，管理页面为 HTTP 3000，DNS 端口为 53335，用户名和密码均为 `admin`。

管理界面跳转地址由当前 YAML 动态决定：HTTP 使用当前 LuCI 页面 URL 的 IP 或域名，并采用 YAML 中的 HTTP 端口；HTTPS 使用 YAML 中配置的 TLS 域名和 HTTPS 端口。

## HTTPS 与 ACME 证书

文件证书路径应写入 YAML 的 `tls.certificate_path` 和 `tls.private_key_path`。`tls.certificate_chain` 与 `tls.private_key` 用于直接内嵌 PEM 内容，不能填写文件路径。

每次通过插件的 `/etc/init.d/AdGuardHome` 启动或重启服务时，插件都会在启动核心前重新读取当前 `config_file` 指向的 YAML，并确保其中配置的证书、私钥及必要父目录可被官方 UID/GID 853 沙箱读取。以后修改 YAML 中的证书路径，也会在下一次启动或重启时按新路径处理。插件只检查可读性，不判断证书是否过期。

ACME 的 `issued`/`renewed` 事件会触发安全重载，使续期证书生效。直接绕过插件调用官方小写 `/etc/init.d/adguardhome`，不会执行插件的启动前证书权限准备。

## 安装与升级

- 全新安装默认不启用服务（`enabled '0'`），在设置页启用并应用后才启动核心和 DNS 集成。默认工作目录缺少 YAML 时安装默认模板；导入已存在的官方实例时保留其启用状态。使用模板时默认选择 `dnsmasq-upstream`；导入既有官方 YAML 时初始使用 `none`，不改变原 DNS 策略。
- `2.4.0-r1` 至 `2.4.0-r9` 均支持原位升级到 `2.4.0-r10`。升级保留当前 UCI、YAML、HTTPS 配置、运行数据和内存模式设置；升级脚本不迁移或删除 2.4 基线以前的历史配置项。
- 安装或升级完成、服务状态恢复后，插件通过 `rpcd reload` 加载新模块，不重启共享 `rpcd` 进程。设置与 YAML 任务会清理无关的继承文件描述符，保留必要的任务锁，不依赖旧进程恰好留有空闲描述符。
- 从 2.3 及更早版本或未知开发版升级不受支持；应先完整卸载旧 LuCI 插件，再安装 2.4.0-r10。官方 `adguardhome` 核心软件包无需卸载。
- 导入既有官方配置时，如果官方 `work_dir` 位于 `/var/*` 或 `/tmp/*`，插件会把 YAML 和现有 `data` 迁移到持久的 `/etc/AdGuardHome`，并把官方 `config_file` 统一为 `/etc/AdGuardHome/AdGuardHome.yaml`；原易失目录保留，便于人工恢复。
- 普通情况下更换已受管的持久工作目录不会搬移旧目录内容：新目录已有 YAML 时直接使用，没有 YAML 时写入默认模板，旧目录保持不动。
- 更换工作目录时同步更新固件升级保留清单，保留当前 YAML 与插件 UCI 快照；不会因为清单仍指向旧目录而漏掉新 YAML。该清单不包含整个 `data`。

核心更新完全交由 ImmortalWrt APK 软件包管理。插件不包含核心下载或更新功能，也不修改官方 APK 的二进制、服务名、UCI 主配置名和包载荷。旧版核心更新器、UPX 与 GFW 列表相关功能均已移除。

## r4 精简

- 设置、YAML、账号修改共用轻量异步任务轮询；移除不再使用的全局 UCI 应用标记。每次“保存并应用”仍统一协调重启，服务停用时保持停用，完成后重新加载所有设置值。
- 日志刷新和 YAML 磁盘重读只显示读取忙碌状态，不再显示“应用配置”倒计时。
- 日常监控保留进程、监听端口和挂载身份检查，不再递归扫描整个内存 data；完整树检查保留在启动、复制和清理边界。
- 删除空操作事务、重复 UCI 检查及 2.4 以前的 RAM YAML 路径修复；合并内存状态、RPC 配置与 YAML 字段的重复解析。
- 超时辅助代码由 `scripts/run-bounded.mk` 单一源码在构建时展开，生成的安装钩子和服务脚本自包含，不新增运行依赖。

## r5 优化

- 核心就绪等待使用轻量设置和挂载身份检查，不再每秒完整扫描内存 `data`；复制、启动、切换和清理边界仍执行完整检查。
- 每轮端口检测只获取一次官方核心进程和文件描述符信息，复用于 TCP/UDP、IPv4/IPv6 判断，不跨轮缓存 PID。
- 首页通过一个状态 RPC 获取服务、内存、DNS 端口和 Web 地址，同一轮复用 UCI 和服务查询；YAML 仍动态读取，轮询不覆盖尚未保存的设置。
- 未改变配置内容及元数据的纯 UCI 事务跳过全系统刷盘；涉及证书权限的流程、实际修改、失败恢复和 data 回写仍保留必要的持久化步骤。
- 应用弹窗改为由任务真实结果结束的等待提示，不再显示固定 90 秒倒计时；页面离开保护及应用后重新加载设置保持不变。
- 合并后台任务状态文件的原子写入实现；安装钩子和初始化脚本的公共权限检查由 `scripts/private-files.mk` 单一源码在构建时展开，不新增运行依赖。

## r6 低开销优化

- 常驻监控间隔由 2 秒改为 5 秒，首页展示轮询由 5 秒改为 10 秒；后台浏览器标签暂停展示查询，恢复可见后继续。启动就绪检查仍为 1 秒，保存应用仍立即协调重启并主动重新加载设置。
- 应用任务查询间隔由 1 秒改为 2 秒，同时将轮数上限改为 180、连续错误上限改为 6；约 6 分钟的间隔预算不包含 RPC 本身耗时。账号 BCrypt 模块仅在打开账号弹窗时加载。
- DNS 状态匹配复用同轮 UCI 加载；只读运行端口查询一次解析所需字段，不再计算未使用的 YAML 哈希。编辑保存、账号修改的内容版本校验继续保留，状态仍动态读取，不跨轮缓存。
- `none` 模式在处理设置及内存状态、清理遗留接管后跳过日常 DNS 端口和 socket 检查；启动与应用时仍验证核心就绪。定时回写周期为 `0` 时跳过计时，正常回写周期和失败后 60 秒重试不变。
- 默认 YAML 只维护 `root/usr/share/luci-app-adguardhome/default.yaml` 一份源码，构建时生成首次安装用的 `/etc/AdGuardHome/AdGuardHome.yaml`。两个安装位置、权限及已有配置保护不变，“恢复模板”仍只更新编辑框。

## r7 低开销与维护优化

- 常驻监控复用本轮已读取的有效回写周期，不再额外启动 UCI 读取；未启用或未处于有效 RAM 模式时不维护回写计时。在线修改周期仍会生效，正常周期及失败后 60 秒重试不变。
- RAM 挂载身份检查合并读取进程 FD 和挂载元数据的两次 AWK 调用，保留全部身份、唯一性和数字校验，不跨轮缓存。
- 锁目录存在时跳过重复创建；RPC 确认 RAM 目录不存在时提前返回，存在但异常的对象仍执行原有检查。
- 首页显示字段不变时保留已有 DOM 节点，减少无效重绘并保留管理链接的键盘焦点；仍每轮查询新状态，不覆盖未保存的设置。
- 升级来源版本清单集中在 `scripts/upgrade-policy.mk`，安装钩子与初始化脚本在构建时展开为自包含脚本。仅接受明确列出的旧版本，不放宽到未知或未来版本，不新增运行依赖。

## r8 低开销与测试维护优化

- RAM 绑定布局检查将两处挂载点及其嵌套挂载检查合为一次挂载表扫描；目录、绑定关系及 inode/device 身份检查保留，复制等操作前后仍分别重新读取。
- 官方 PID 首行筛选和父进程 `PPid` 字段使用 shell 内建读取，减少小型 AWK 子进程；不缓存 PID，保留进程归属、数字格式及父链深度校验。
- 保存应用期间暂停当前设置页的例行状态查询，复用已有提交状态；任务进度查询继续，结束后在页面仍有效时刷新状态，不覆盖设置草稿或改变真实任务结果。
- 相同的 shell 测试函数提取逻辑集中到测试辅助文件，减少测试维护重复；辅助文件不会安装到路由器，不新增运行依赖。
- 监控、首页和应用任务查询间隔保持 5 秒、10 秒、2 秒；正常与定时 data 回写仍直接覆盖，不重启核心或服务。

## r9 审计修复与精简

- 启停过程的 UID 检查改用 shell 内建读取，不再对每个线程启动 AWK；仍检查所有线程的四个 UID 字段，保留格式、不可读及进程消失处理。
- 连续稳定就绪后直接返回，删除紧邻的重复配置和监听检查；DNS 集成应用前后的实时验证保持不变。
- 启用后补建唯一的 procd monitor，禁用后撤销该实例；重复应用不替换已有 monitor，周期回写不重启核心或服务。
- 设置与 YAML 任务共用继承 FD 校验和清理，普通 APK 升级只 reload rpcd，不以全局重启绕过任务锁失败。
- DNS/防火墙操作发现默认 CLI 暂存区有未提交修改时拒绝更改，不顺带提交或回滚其他设置；无须清理时仍直接返回。
- 当前 YAML 的固件备份清单采用单一源码生成，不变不写，工作目录变更后同步替换旧路径。
- HTTP 回环判断使用 ucode 内建 IP 解析，补齐 IPv4-mapped、展开及带作用域的 IPv6 回环地址。
- 删除无调用的旧函数，复用目录和 inode 校验，去掉重复 UCI 加载、未用端口参数及管理链接计算。YAML 保存响应丢失时保留草稿，并要求重新读取磁盘版本后再提交。
- 不新增依赖、常驻进程或定时器；官方核心、服务文件和原有权限边界保持不变。

## r10 更新

- 全新安装默认关闭服务，升级保留原来的启用状态。
- 修复 CLI 中有未提交 UCI 修改时，监控可能停止正常核心的问题。待提交期间跳过本轮处理，不改动用户的暂存配置。
- 修复新工作目录首次开启内存模式时的目录访问权限，以及内存模式下切换工作目录的 YAML 初始化顺序。
- 修复任务文件描述符与内部临时描述符冲突导致保存失败的问题。
- 修复升级初始化失败时，错误恢复最初官方配置的问题。
- 状态查询失败时显示“不可用”，不再误报“未运行”；为 YAML 和日志文本框补充可访问名称。
- 自定义工作目录先检查现有 UCI 路径，减少重复的内存状态和挂载检查。
- 合并重复的私有文件校验，改用 ucode 内置随机文件读取和数组逆序，删除重复的管理链接校验、未使用的模板哈希和无恢复用途的 `.uci` 导出副本。原始配置备份及保存冲突保护继续保留。
